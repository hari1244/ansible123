- name: loops remove
  hosts: localhost
  connection: local
  become: yes
  tasks:
  - name: remove packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: absent
    loop:
      - nginx
      - mysql
      - zip